import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["w7.pngwing.com", "cdn.sanity.io"], // Agrega el dominio de la imagen externa
  },
};

export default nextConfig;
